import React from 'react';
import  './Gradient.scoped.scss';

const Gradient = props => {
    return (
        <div className="challenge">
            <div className="grad grad-1"></div>
            <div className="grad grad-2"></div>
        </div>
    );
}

export default Gradient;
